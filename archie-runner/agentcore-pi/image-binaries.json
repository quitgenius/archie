{
  "_note": "Binaries present in the agentcore-pi runtime image (node:22-slim base + this dir's Dockerfile apt installs and COPYs). KEEP IN SYNC WITH THE Dockerfile. The G7 coherence validator (config-resolver/validate-requires.mjs) checks every skill's requires.exec.safeBinProfiles against this list; a skill that needs a binary not here is a hard finding \u2014 either add the binary to the Dockerfile AND this list, or port the skill off the shell. git is intentionally absent (git-free boot; config comes from DDB), which is why understand/understand-diff/create-agent/agent-u0yebw are PI_INCOMPATIBLE_SKILLS. aws + curl + jq added 2026-09-08: the five AWS/observability skills were reverted from in-process tools to their shell scripts, so the image has to provide what those scripts invoke. `aws` is CLI v2, COPYd from amazon/aws-cli and symlinked onto PATH.",
  "binaries": [
    "bash",
    "sh",
    "tar",
    "gzip",
    "node",
    "npm",
    "python3",
    "make",
    "g++",
    "curl",
    "jq",
    "aws"
  ]
}
